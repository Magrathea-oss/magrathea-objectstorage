package com.example.magrathea.storageengine.application.pipeline;

/** Marker interface for encryption step implementations (transform, 1:1). */
public interface EncryptionStep extends DataProcessingStep {}
