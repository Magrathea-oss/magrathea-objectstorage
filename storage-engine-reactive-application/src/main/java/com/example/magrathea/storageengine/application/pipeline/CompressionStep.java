package com.example.magrathea.storageengine.application.pipeline;

/** Marker interface for compression step implementations (transform, 1:1). */
public interface CompressionStep extends DataProcessingStep {}
