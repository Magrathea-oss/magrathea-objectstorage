# Handoff request: documenter → java-planner

## Context

documenter needs to copy static resources and regenerate ARC42 JSON, but all paths under `bootstrap-application/` are owned by `java-infra-coder`. documenter's only direct child is `c4model`, which also cannot write to `bootstrap-application/`.

## Required Steps

Delegate to `java-infra-coder` to execute the following under `/home/paperboy/workspace/magrathea-objectstorage`:

### 1 — Copy C4 images

First delegate to `c4model` to confirm source files, then delegate to `java-infra-coder`:

```bash
mkdir -p bootstrap-application/src/main/resources/static/docs/c4/images/
cp docs/c4/images/*.png bootstrap-application/src/main/resources/static/docs/c4/images/
echo "C4 images: $(ls bootstrap-application/src/main/resources/static/docs/c4/images/ | wc -l)"
```

### 2 — Copy arc42-logo

```bash
mkdir -p bootstrap-application/src/main/resources/static/docs/arc42/images/
cp docs/arc42/images/arc42-logo.png bootstrap-application/src/main/resources/static/docs/arc42/images/
echo "arc42-logo copied"
```

Source exists: `docs/arc42/images/arc42-logo.png` (8151 bytes, confirmed by documenter).

### 3 — Regenerate ARC42 JSON

```bash
node bootstrap-application/src/main/scripts/asciidoc-to-arc42-json.mjs 2>&1
python3 -c "
import sys,json; d=json.load(open('bootstrap-application/src/main/resources/static/docs/arc42.json'))
img_count = sum(1 for s in d['document']['sections'] for b in s.get('blocks',[]) if b['type'] == 'image')
print(f'Images: {img_count}')
for s in d['document']['sections']:
    for b in s.get('blocks',[]):
        if b['type'] == 'image':
            print(f'  src={b.get(\"src\",\"\")[:80]} alt={b.get(\"alt\",\"\")[:60]}')
"
```

### 4 — Verify

```bash
echo '=== C4 images ==='
ls -la bootstrap-application/src/main/resources/static/docs/c4/images/
echo '=== arc42 images ==='
ls -la bootstrap-application/src/main/resources/static/docs/arc42/images/
echo '=== arc42.json image count ==='
python3 -c "
import sys,json; d=json.load(open('bootstrap-application/src/main/resources/static/docs/arc42.json'))
cnt = sum(1 for s in d['document']['sections'] for b in s.get('blocks',[]) if b['type'] == 'image')
print(cnt)
"
```

## After completion

Handoff back to documenter so I can verify results and proceed with the workflow. The workflow should then continue with java-infra-coder for Docker rebuild.
