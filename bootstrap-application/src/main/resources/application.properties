# Default active profile: in-memory (single-node) backend.
# Switch to storage-engine backend by activating the storage-engine profile:
#   java -jar app.jar --spring.profiles.active=storage-engine
spring.profiles.default=single-node
