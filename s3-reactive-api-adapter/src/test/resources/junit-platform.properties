# Restrict Cucumber classpath scanning to the glue package
# Without this, Cucumber scans the entire classpath, producing
# NoClassDefFoundError warnings for unrelated classes and
# potentially missing scenarios.
# Glue is configured per runner via @ConfigurationParameter on test classes
# See AwsCliCucumberTest and ObjectStoreCucumberTest
cucumber.plugin=pretty
